{
  "capture": "manual",
  "claudeCodeVersion": "2.1.210",
  "request": {
    "method": "POST",
    "path": "/v1/messages",
    "stream": true,
    "topLevelKeys": [
      "context_management",
      "max_tokens",
      "messages",
      "metadata",
      "model",
      "output_config",
      "stream",
      "system",
      "thinking",
      "tools"
    ]
  },
  "envelopeObservation": "The captured top-level request envelope matches the long tool-enabled ordinary request. Nested non-text structure was not compared.",
  "textObservation": "The final user block contains a natural-language summarization instruction. Prompt text is not a protocol discriminator."
}
